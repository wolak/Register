public abstract class Product {
	/* Returns the total cost of a particular item based on the total number(itemCount) of occurences of that item in the items list
	 @param		itemCount	Number of occurences of the item in the items list*/
	public abstract int calculateTotal(int itemCount);
}